// vite.config.ts
import { defineConfig } from 'vite'
// ... diğer import'lar

export default defineConfig({
  // ... diğer ayarlar
  base: '/balconyflowersvcard/', // 👈 Depo adınızın başlangıcına ve sonuna eğik çizgi (slash) ekleyin
})